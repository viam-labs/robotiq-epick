# Toolchain for `make fit-primitives`. Not a build or runtime dependency of the
# module -- it only re-derives the primitive table in epick/geometry.go and
# epick/epick_model.json from the CAD exports under epick/meshes/, and writes
# no source file.
#
# networkx is never imported directly, but trimesh reaches it through
# mesh.split(), which is how the fit separates the export into components.
trimesh==4.12.2
networkx==3.6
numpy==2.5.1
